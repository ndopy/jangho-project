services:
  db:
    image: postgres:15
    container_name: jangho_postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: jangho_user
      POSTGRES_PASSWORD: jangho_password
      POSTGRES_DB: jangho_db
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
